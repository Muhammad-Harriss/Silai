import 'package:flutter/material.dart';


class AppColors {
  // 🌿 Primary Theme Colors
  static const Color primary = Color(0xFF2D7A4F); // Deep Green (trust, reliability)
  static const Color primaryLight = Color(0xFF4CAF50); // Light Green (success tone)
  static const Color accent = Color(0xFFD4AF37); // Golden (premium touch)

  // 🌤 Backgrounds
  static const Color background = Color(0xFFF5F5F5); // Soft neutral
  static const Color card = Color(0xFFFFFFFF); // White card background
  static const Color surface = Color(0xFFFAFAFA);

  // 🖋 Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Color(0xFFFFFFFF);

  // 💬 Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // 🧵 Payment/Order Status Indicators
  static const Color completed = Color(0xFF2E7D32); // Done suits or paid clients
  static const Color pending = Color(0xFFE65100);   // Pending suits or payments

  // 🌑 Dark Mode Support
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkCard = Color(0xFF1E1E1E);
  static const Color darkTextPrimary = Color(0xFFFFFFFF);
  static const Color darkTextSecondary = Color(0xFFB0B0B0);

  // ✨ Borders & Dividers
  static const Color border = Color(0xFFE0E0E0);
  static const Color shadow = Colors.black12;
}
